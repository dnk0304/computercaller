<?xml version="1.0" encoding="utf-8"?>
<!-- Hero state card. DIRECTION trait 2: separation by surface + a 1dp
     hairline, never elevation. Radius 20 per the token block. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/surface_raised" />
    <stroke android:width="1dp" android:color="@color/surface_border" />
    <corners android:radius="@dimen/cc_radius_hero" />
</shape>
